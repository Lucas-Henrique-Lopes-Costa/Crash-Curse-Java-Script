<!DOCTYPE html>
<html lang="en">

<%- include('../partials/head'); %>

  <body class="container">
    <%- include('../partials/header', {pagina: 'inicial' }) %>
      <main>
        <div class="jumbotron">
          <h1>Node com EJS</h1>
          <p>Vamos aprender a trabalhar ocm arquivos EJS</p>
        </div>
      </main>
      <%- include('../partials/footer') %>
        <ul>
          <% item.forEach(function(items) { %>
            <li>
              <strong>
                <%= items.title %>
              </strong>: <%= items.message%>
            </li>
            <% }) %>
        </ul>
  </body>

</html>